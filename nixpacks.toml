[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m ensurepip --upgrade && python -m pip install -r Scamurai/backend/requirements.txt"]

[start]
cmd = "python -m uvicorn backend.main:app --app-dir Scamurai --host 0.0.0.0 --port ${PORT}"
