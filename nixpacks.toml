[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r Scamurai/backend/requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --app-dir Scamurai --host 0.0.0.0 --port ${PORT}"
